{
  "routes": [
    { "src": "/api/(.*)", "dest": "https://api.grandebeach.com/api/$1" },
    { "src": "/uploads/(.*)", "dest": "https://api.grandebeach.com/uploads/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
